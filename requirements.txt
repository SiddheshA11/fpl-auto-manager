requests>=2.31.0,<3.0.0
# Upper bounds are deliberate. This bot submits real transfers unattended, and
# CI resolves these fresh on every run - so an unpinned major release lands in
# production without anyone choosing it. pandas 3.0 did exactly that: it gave
# text columns a dedicated string dtype, which silently falsified a
# `dtype == object` check in priors.py, stripped the position off every player
# prior and produced a squad worth half what it should have been. It passed
# locally on pandas 2.2 the whole time. Raise these deliberately, with a run of
# the suite against the new major, rather than discovering it at a deadline.
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0
# scipy provides both the HiGHS solver behind optimizer.py and the count
# distributions the xP model uses; nothing else pulls in a solver.
scipy>=1.9.0,<2.0.0
# GitHub secret encryption for refresh-token rotation.
pynacl>=1.5.0,<2.0.0
# Browser automation for the FPL login flow in token_refresh.py.
playwright>=1.40.0,<2.0.0
pytest>=7.0.0,<9.0.0
